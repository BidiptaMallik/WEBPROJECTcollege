<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Daily Routine</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .glass-effect {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
        }
        .timeline-line::before {
            content: '';
            position: absolute;
            top: 0;
            bottom: 0;
            left: 24px;
            width: 2px;
            background: #e5e7eb;
            z-index: 0;
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-800">

    <nav class="bg-white border-b border-gray-200 sticky top-0 z-40">
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <a href="/main" class="text-xl font-bold text-blue-600 flex items-center gap-2">
                        <i class="fas fa-arrow-left text-sm"></i> Dashboard
                    </a>
                </div>
                <div class="flex items-center space-x-4">
                    <span class="text-sm text-gray-500">Welcome, <%= user ? user.fullname : 'User' %></span>
                    <a href="/logout" class="text-sm font-medium text-red-500 hover:text-red-700">Logout</a>
                </div>
            </div>
        </div>
    </nav>

    <div class="bg-blue-600 text-white pb-32 pt-12">
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-end">
                <div>
                    <h2 class="text-sm uppercase tracking-wide font-medium text-blue-200 mb-1">Daily Overview</h2>
                    <h1 class="text-3xl font-bold"><%= typeof date !== 'undefined' ? date : new Date().toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></h1>
                    <p class="mt-2 text-blue-100">Stay consistent. Small steps every day.</p>
                </div>
                <div class="hidden sm:block">
                    <div class="bg-blue-700/50 rounded-lg p-3 text-center backdrop-blur-sm border border-blue-500/30">
                        <span class="block text-2xl font-bold">85%</span>
                        <span class="text-xs text-blue-200">Consistency</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 -mt-24 pb-12 relative z-10">
        <div class="bg-white rounded-xl shadow-lg border border-gray-100 p-6 sm:p-8 min-h-[500px]">
            
            <div class="flex items-center justify-between mb-8">
                <h3 class="text-lg font-bold text-gray-900">Today's Schedule</h3>
                <button onclick="toggleModal('addTaskModal')" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors shadow-sm flex items-center gap-2">
                    <i class="fas fa-plus"></i> Add Task
                </button>
            </div>

            <div class="relative timeline-line space-y-8">
                <% if (routine && routine.length > 0) { %>
                    <% routine.forEach(function(item) { %>
                        <div class="relative pl-16 group">
                            <div class="absolute left-0 top-1 w-12 h-12 rounded-full border-4 border-white shadow-sm flex items-center justify-center z-10 
                                <%= item.status === 'active' ? 'bg-blue-600 text-white ring-2 ring-blue-200' : 
                                   (item.status === 'done' ? 'bg-green-100 text-green-600' : 'bg-gray-100 text-gray-500') %>">
                                <span class="text-xl"><i class="<%= item.icon || 'fas fa-clock' %>"></i></span>
                            </div>

                            <div class="bg-white rounded-lg border border-gray-100 p-4 hover:shadow-md transition-shadow duration-200 
                                <%= item.status === 'active' ? 'border-l-4 border-l-blue-600 bg-blue-50/30' : '' %>">
                                
                                <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-2">
                                    <div class="flex items-center gap-3">
                                        <span class="font-mono text-sm font-semibold text-gray-500 bg-gray-100 px-2 py-1 rounded">
                                            <%= item.time %>
                                        </span>
                                        <h4 class="font-bold text-gray-900 <%= item.status === 'done' ? 'line-through text-gray-400' : '' %>">
                                            <%= item.title %>
                                        </h4>
                                    </div>
                                    <span class="inline-flex mt-2 sm:mt-0 items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                                        <%= item.category === 'Work' ? 'bg-purple-100 text-purple-800' : 
                                           (item.category === 'Health' ? 'bg-green-100 text-green-800' : 
                                           (item.category === 'Growth' ? 'bg-yellow-100 text-yellow-800' : 'bg-gray-100 text-gray-800')) %>">
                                        <%= item.category %>
                                    </span>
                                </div>

                                <p class="text-gray-600 text-sm mb-3 ml-1"><%= item.description %></p>

                                <div class="flex gap-2 ml-1 items-center">
                                    <form action="/routine/toggle/<%= item._id %>" method="POST" class="inline">
                                        <button type="submit" class="text-xs px-3 py-1 rounded-md border transition-colors
                                            <%= item.status === 'done' 
                                                ? 'bg-gray-100 text-gray-500 border-gray-200 hover:bg-gray-200' 
                                                : 'bg-green-50 text-green-600 border-green-200 hover:bg-green-100' %>">
                                            <%= item.status === 'done' ? 'Undo' : 'Mark Done' %>
                                        </button>
                                    </form>

                                    <form action="/routine/delete/<%= item._id %>" method="POST" class="inline" onsubmit="return confirm('Are you sure you want to delete this task?');">
                                        <button type="submit" class="text-xs text-gray-400 hover:text-red-600 px-2 py-1 transition-colors" title="Delete Task">
                                            <i class="fas fa-trash-alt"></i>
                                        </button>
                                    </form>
                                </div>

                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="mt-20 text-center">
                        <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-blue-50 text-blue-200 mb-4">
                            <i class="fas fa-check text-2xl"></i>
                        </div>
                        <h3 class="text-lg font-medium text-gray-900">No tasks yet</h3>
                        <p class="text-gray-500 text-sm mt-1">Add a task to start your day!</p>
                    </div>
                <% } %>
            </div>

            <% if (routine && routine.length > 0) { %>
                <div class="mt-12 text-center border-t border-gray-100 pt-8">
                    <p class="text-gray-400 text-sm italic">That's all for today. Great job!</p>
                </div>
            <% } %>

        </div>
    </div>

    <div id="addTaskModal" class="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full hidden z-50">
        <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
            <div class="flex justify-between items-center mb-4">
                <h3 class="text-lg font-bold text-gray-900">Add New Task</h3>
                <button onclick="toggleModal('addTaskModal')" class="text-gray-400 hover:text-gray-600">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            
            <form action="/routine/add" method="POST">
                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Title</label>
                        <input type="text" name="title" required class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="e.g., Morning Jog">
                    </div>
                    
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Time</label>
                            <input type="time" name="time" required class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Category</label>
                            <select name="category" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                                <option value="General">General</option>
                                <option value="Work">Work</option>
                                <option value="Health">Health</option>
                                <option value="Growth">Growth</option>
                            </select>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Icon (FontAwesome Class)</label>
                        <input type="text" name="icon" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="fas fa-running" value="fas fa-circle">
                        <p class="text-xs text-gray-500 mt-1">e.g., 'fas fa-book', 'fas fa-dumbbell'</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Description</label>
                        <textarea name="description" rows="2" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Optional details..."></textarea>
                    </div>

                    <button type="submit" class="w-full bg-blue-600 text-white font-bold py-2 px-4 rounded-md hover:bg-blue-700 transition duration-300">
                        Add Task
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script>
        function toggleModal(modalID) {
            document.getElementById(modalID).classList.toggle("hidden");
        }
        
        window.onclick = function(event) {
            const modal = document.getElementById('addTaskModal');
            if (event.target == modal) {
                modal.classList.add("hidden");
            }
        }
    </script>
</body>
</html>